# C++ 编程笔记：素数判断的边界逻辑与优化

### 1. 核心困惑：为什么循环边界是 `j < i - 1`？
在判断素数的代码中，内层循环通常用于检查 @@MATH_BLOCK_0@@ 是否能被 @@MATH_BLOCK_1@@ 整除。
* **疑问点**：通常认为要排除掉 @@MATH_BLOCK_2@@ 本身，边界应该是 `j < i`。那么 `j < i - 1` 漏掉了 @@MATH_BLOCK_3@@ 这个数，结果还准确吗？

---

### 2. 深度解析：三种边界条件的对比

| 边界条件 | 是否正确 | 逻辑说明 |
| :--- | :--- | :--- |
| **`j <= i`** | **错误** | 因为 @@MATH_BLOCK_4@@ 永远等于 0，会导致所有数字都被判定为“非素数”。 |
| **`j < i`** | **正确** | 标准逻辑。检查 @@MATH_BLOCK_5@@ 到 @@MATH_BLOCK_6@@ 之间是否有约数，排除了 @@MATH_BLOCK_7@@ 本身。 |
| **`j < i - 1`** | **正确** | **本代码采用的逻辑**。利用了数学特性：相邻自然数互质。 |

#### 为什么 `j < i - 1` 是可行的？
* **数学原理**：对于任何大于 2 的整数 @@MATH_BLOCK_8@@，除以 @@MATH_BLOCK_9@@ 的余数永远是 **1**（即 @@MATH_BLOCK_10@@）。
* **结论**：既然 @@MATH_BLOCK_11@@ 永远不可能整除 @@MATH_BLOCK_12@@，那么在循环中查不检查这个数，都不会改变 `flag` 的结果。

---

### 3. 进阶：更高效的“平方根”判别法
虽然 `j < i - 1` 逻辑正确，但在处理大数据时效率较低（时间复杂度 @@MATH_BLOCK_13@@）。

**更好的写法：**
```cpp
for(int j = 2; j * j <= i; j++)
```
**原理**：
如果一个数 @@MATH_BLOCK_14@@ 有因数，它们一定是成对出现的（例如 100 的因数：@@MATH_BLOCK_15@@，@@MATH_BLOCK_16@@，@@MATH_BLOCK_17@@）。
* 在一对因数中，必然有一个小于或等于 @@MATH_BLOCK_18@@。
* 如果查到 @@MATH_BLOCK_19@@ 都没有发现因数，那么大于 @@MATH_BLOCK_20@@ 的地方也绝对不会有了。



---

### 4. 总结与建议
1.  **避开自身**：判断素数时，循环上限绝对不能包含 @@MATH_BLOCK_21@@ 本身。
2.  **理解冗余**：`j < i - 1` 相比 `j < i` 只是少了一次注定失败的尝试，虽然可行但不常用。
3.  **标准实践**：在实际编程或竞赛中，推荐使用 `j * j <= i` 或 `j <= sqrt(i)`，这是性能与简洁性的最优平衡。