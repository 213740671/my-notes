# 题目
![[Pasted image 20260114164305.png]]
# 代码
## 数组方式
```cpp
#include <bits/stdc++.h>  
using namespace std;  
  
int main() {  
    int a[10];  
    for (int i=0;i<10;i++) {  
        cin>>a[i];  
    }  
    for (int i=9;i>=0;i--) {  
        cout<<a[i];  
        if (i!=0)  
            cout<<" ";  
    }  
}
```
## 栈的方式(先进后出)
```cpp
#include <bits/stdc++.h>  
using namespace std;  
stack<int>s;  
  
int main() {  
  
    for (int i=0;i<10;i++) {  
        int a;  
        cin>>a;  
        s.push(a);  
    }  
    for (int i=9;i>=0;i--) {  
        if (s.empty())  
            break;  
        cout << s.top();  
        s.pop();  
        if (i!=0)  
            cout<<" ";  
    }  
}
```
