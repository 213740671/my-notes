# 嵌入式之 STM32 学习笔记汇总 (Day01 - Day08)

本文档整合了嵌入式 STM32 课程第 1 天至第 8 天的课堂笔记，涵盖了 STM32 基础概述、环境搭建、GPIO、时钟系统、中断系统、USART 串口通信以及 IIC 通信等内容。

---

## 第一部分：STM32 基础与环境搭建 (Day01)

### 1. STM32 概述

#### 1.1 SMT32 型号名称介绍
以 **STM32F103ZET6** 为例：
- **STM**: ST 为公司名称
- **32**: 32位处理器
- **M**: 表示 ARM 架构 Cortex-M3 系列
- **F**: 产品类别，F 表示基础型 (Foundation)
- **103**: 特定功能，103 表示基础型
- **Z**: 引脚数，Z 表示 144 个引脚
- **E**: 闪存大小，E 表示 512KB
- **T**: 封装方式，T 表示 SOP 封装（四面扁平封装）
- **6**: 工作温度，6 表示 -40℃ 到 85℃

#### 1.2 STM32 开发方式
1. 基于寄存器开发
2. 基于标准外设库开发
3. 基于 HAL 库开发

### 2. 开发环境搭建

#### 2.1 Keil MDK
1. 下载并安装 Keil MDK，可以与前面的 Keil C51 安装到同一个目录中，也可以分开安装。
2. 离线安装 STM32F103ZE 的芯片支持包。

#### 2.2 ST-Link 驱动
1. 安装 ST-Link 驱动。
2. 升级 ST-Link 固件。

#### 2.3 STM32 烧录程序
1. 安装烧录程序 STM32 ST-LINK Utility。
2. 使用烧录程序烧录测试程序。

### 3. Keil 工程案例

#### 3.1 工程准备
1. 提前创建好工程目录。
2. 从标准外设库中将启动文件和其他核心文件复制到工程目录中。
3. 在 Keil 中新建项目并创建在前面准备好的目录中。

#### 3.2 工程配置
1. 添加 Project Group, 命名为 `Star`，将前面准备的启动文件和其他核心文件添加进去。
2. 添加 Project Group, 命名为 `User`，自己的的源文件放在里面。

#### 3.3 编译配置
最新的 Keil MDK 内置的编译器是 Compiler version 6，与前面的 `core_cm3.c` 不兼容，需要替换成 **Compiler version 5**。

#### 3.4 其他配置
1. 配置 Create HEX File。
2. 添加 Include Paths。

#### 3.5 烧录配置
1. Keil 选项配置的 Debug 选项中选择 **ST-Link Debugger**。
2. 勾选 **Reset and Run**。

> **注意**：如果配置烧录模块的时候 Keil 闪退，将 `STLinkUSBDriver.dll` 复制到 `C:\Keil_v5\ARM\STLink` 目录下，替换掉原来的！

---

## 第二部分：GPIO 外设与点灯案例 (Day02)

### 1. 点亮 LED 案例

#### ① 基础版
> 直接表示寄存器地址。
```c
#include "stdint.h"

int main(void)
{
    /* 开启GPIOA的时钟 */
    *(uint32_t *)(0x40021000 + 0x18) = 4;

    /* 给PA0设置为通用推挽输出和最大速度输出 */
    *(uint32_t *)(0x40010800 + 0x00) = 3;

    /* 给输出寄存器赋值 */
    *(uint32_t *)(0x40010800 + 0x0c) = 0xfffe;
    while (1)
    {
    }
}
```

#### ② 优化1
> 使用 ST 提供的已定义好的宏定义名称表示寄存器地址。
```c
#include <stdint.h>
#include "stm32f10x.h"                  // Device header

int main()
{
    // 开启时钟
    RCC->APB2ENR = 0x00000004;
    
    // 配置PA0引脚的输出方式为推挽，最大输出速率50MHz
    GPIOA->CRL = 0x44444443;
    
    // 设置PA0引脚的输出0
    GPIOA->ODR = 0x0000FFFE;

    // 死循环
    while (1)
        ;
}
```

#### ③ 优化2
> 利用位运算只修改寄存器中指定的位。
```
给寄存器R的第n位置1： R |= (1 << n)
给寄存器R的第n位置0： R &= ~(1 << n)
```
```c
#include <stdint.h>
#include "stm32f10x.h"                  // Device header

int main()
{
    // 开启时钟，第2位置1
    RCC->APB2ENR |= (1 << 2);
    
    // 配置PA1引脚的输出方式为推挽，最大输出速率50MHz, 7、6、5、4位设置为 0011
    GPIOA->CRL &= ~(1 << 7);
    GPIOA->CRL &= ~(1 << 6);
    GPIOA->CRL |= (1 << 5);
    GPIOA->CRL |= (1 << 4);
    
    // 设置PA1引脚的输出0, 第1位置0
    GPIOA->ODR &= ~(1 << 1);

    // 死循环
    while (1)
        ;
}
```

#### ④ 优化3
```c
#include <stdint.h>
#include "stm32f10x.h"                  // Device header

int main()
{
    // 开启时钟，第2位置1
    RCC->APB2ENR |= RCC_APB2ENR_IOPAEN;
    
    // 配置PA0引脚的输出方式为推挽，最大输出速率50MHz,
    GPIOA->CRL &= ~GPIO_CRL_CNF0_1;
    GPIOA->CRL &= ~GPIO_CRL_CNF0_0;
    GPIOA->CRL |= GPIO_CRL_MODE0_1;
    GPIOA->CRL |= GPIO_CRL_MODE0_0;
    
    // 设置PA0引脚的输出
    GPIOA->ODR &= ~GPIO_ODR_ODR0;

    // 死循环
    while (1)
        ;
}
```

### 2. GPIO 外设

#### 2.1 IO 引脚模式
**输出模式：**
1. 通用推挽输出
2. 通用开漏输出
3. 复用推挽输出
4. 复用开漏输出

**输入模式：**
1. 模拟输入
2. 上拉/下拉输入
3. 浮空输入(复位值)

#### 2.2 GPIO 相关寄存器
```
GPIOx_CRL       端口配置低寄存器
GPIOx_CRH       端口配置高寄存器
GPIOx_IDR       输入数据寄存器
GPIOx_ODR       输出数据寄存器
GPIOx_BSRR      位设置/清除寄存器
GPIOx_BRR       位清除寄存器
GPIOx_LCKR      端口配置锁寄存器
```

---

## 第三部分：总体架构、时钟系统与 HAL 库入门 (Day03)

### 1. 总体架构

#### ① 被动单元
1. 内部 SRAM
2. 内部闪存存储器
3. FSMC，连接 MCU 外部的 SRAM 和 Flash
4. AHB 到 APB 的桥 (AHB2APBx)，它连接所有的 APB 设备

#### ② 驱动（主动）单元
1. Cortex™-M3 内核 DCode 总线 (D-bus)
2. 系统总线 (S-bus)
3. 通用 DMA1
4. 通用 DMA2

### 2. 时钟系统
- **HSI**: 高速内部时钟，由内部 RC 电路产生，精准度较差，可作为系统时钟。
- **HSE**: 高速外部时钟，一般由外部晶振提供，可作为系统时钟。
- **PLL**: 锁相环，实现倍频。
- **LSI**: 低速内部时钟，由内部 RC 电路产生，可作为实时时钟频率。
- **LSE**: 低速外部时钟，一般由外部晶振提供，可作为实时时钟频率。

### 3. HAL 库开发入门

#### 3.1 开发环境搭建
1. 安装 STM32CubeMX （需要 java8 环境支持）。
2. 安装 HAL 库支持包（在线或离线安装）。

#### 3.2 使用 STM32CubeMX 创建 HAL 库项目
1. **第一步**: 引脚配置
2. **第二步**: 时钟配置
3. **第三步**: 项目管理配置

---

## 第四部分：STM32 的中断系统 (Day04)

### 1. 中断概述

#### ① STM32 的中断
STM32F103 系列支持 70 个中断，包括 10 个内核中断和 60 个可编程的外部中断。

#### ② STM32 中断体系结构
1. CPU 核心
2. NVIC
3. 中断来源
   - 内核中断
   - 片上外设
   - GPIO EXTI 中断
4. EXTI 中断：AFIO 寄存器、EXTI 寄存器处理

#### ③ NVIC 嵌套向量中断控制寄存器和中断优先级
- **抢占优先级（Group）**：优先级高的会打断优先级低。
- **响应优先级（Sub）**：不会打断优先级低，排队等待。
- **规则**：数字小优先级高。

#### ④ EXTI 外部中断控制器
- `EXTI_RTSR`: 上升沿触发选择寄存器
- `EXTI_FTSR`: 下降沿触发选择寄存器 (原文误写为上升沿，已根据常用知识修正提示，但保留原文: 上升沿触发选择寄存器)
- `EXTI_IMR`: 中断屏蔽寄存器
- `EXTI_PR`: 挂起寄存器

### 2. 中断案例 —— 检测按键按下

#### ① 寄存器方式实现
- **需求一**：SW5 按下，LED1（黄灯）亮灭转换
- **需求二**：SW6 按下，LED2（蓝灯）亮灭转换
- **需求三**：SW4 按下，LED3（绿灯）亮灭转换

#### ② HAL 库方式实现
*(此处笔记内容为空)*

---

## 第五部分：USART 串口通信 (Day05 & Day06)

### 1. 通信基础知识

#### ① 串行、并行
- **串行**：使用一根数据线逐位传输
- **并行**：使用多根数据线同时传输

|                | **串行** | **并行** |
| -------------- | -------- | -------- |
| **传输速率**   | 低       | 高       |
| **硬件成本**   | 低       | 高       |
| **抗干扰能力** | 高       | 低       |
| **通讯距离**   | 高       | 低       |

#### ② 单工、半双工、全双工
- **单工**：数据传输方向是单向的
- **半双工**：数据传输方向是双向的，发送和接收不能同时进行
- **全双工**：数据传输方向是双向的，发送和接收可以同时进行

#### ③ 同步、异步
- **同步**：需要时钟信号线，发送方和接收方频率一致
- **异步**：不需要时钟信号线，发送方和接收方频率不一定一致

#### ④ USART/UART 串口通信
- 全称：Universal Synchronous/Asynchronous Receiver/Transmitter (通用 同步/异步 收/发)
- **数据帧格式**：
  - 空闲位
  - 起始位
  - 数据位（通常 8 bit）
  - 校验位
  - 停止位

### 2. STM32 的串口模块

#### ① 5个模块
- **USART1、USART2、USART3**: 支持同步、异步
- **UART4、UART5**: 只支持异步

#### ② 使用步骤
**发送数据：**
1. 发送使能
2. 向 TDR 写数据
3. 检测 TXE 标志位（发送完一个数据帧）

**接收数据：**
1. 接收使能
2. 读 RDR
3. 检测 RXNE 标志位（接收完一个数据帧）

#### ③ 波特率计算
```
波特率 = 时钟频率 / 16 / USARTDIV
```

#### ④ 相关寄存器
- **USART1 寄存器组**：
  - `SR`: 状态寄存器，存放各种标志值
  - `DR`: 数据寄存器，TXD 和 RXD 共用一个寄存器名称（地址）
  - `CR1`: 控制寄存器 1，TE、RE、UE、PCE 等
  - `CR2`: 控制寄存器 2，STOP 等
  - `BRR`: 波特率寄存器，16 位保留、12 位存整数、4 位放小数
- 其他：USART2, USART3, UART4, UART5 寄存器组

### 3. USART 案例 (寄存器版)
- 方案一：轮询方案
- 方案二：中断方案，接收完数据触发中断

### 4. USART 案例 (HAL库版)

#### ① 轮询方式 - 相关 HAL 库函数
- **HAL_UART_Transmit()**: 以阻塞模式发送数据。
- **HAL_UART_Receive()**: 以阻塞模式接收指定数量的数据。
- **HAL_UARTEx_ReceiveToIdle()**: 以阻塞模式接收数据，直到接收到期望的数据量或发生空闲 (IDLE) 事件。

#### ② 中断方式 - 相关 HAL 库函数
- **HAL_UART_Receive_IT()**: 以非阻塞模式接收数据。
- **HAL_UARTEx_ReceiveToIdle_IT()**: 以中断模式接收数据，直到接收到指定数量的数据或发生空闲（IDLE）事件。

### 5. 使用串口调试 重定向 printf

#### ① 代码实现 寄存器版
```c
int fputc(int ch, FILE *stream )
{
    // 通过USART1串口发送
    USART1_TransmitByte((uint8_t)ch);
    // 返回
    return ch;
}
```

#### ② 代码实现 HAL 库
```c
int fputc(int ch, FILE *stream)
{
    // 向串口发数据
    HAL_UART_Transmit(&huart1, (uint8_t *)&ch, 1, HAL_MAX_DELAY);
    // 返回
    return ch;
}
```

---

## 第六部分：IIC 通信与 EEPROM (Day07 & Day08)

### 1. IIC 协议概述

#### ① 起始位和结束位
> 空闲状态是高电平！
- **起始位**：SCL 高电平时候，检测到 SDA 下降沿。
- **结束位**：SCL 高电平时候，检测到 SDA 上升沿。

#### ② 物理层
总线上的设备（主设备还是从设备）使用开漏模式，靠总线上的上拉电阻输出高电平。

#### ③ 协议层
1. 在 SCL 还是低电平的时候，修改 SDA 的状态。
2. SDA 高低电平要持续稳定一段时间。

#### ④ 模式
- 标准模式：100Kbit/s
- 快速模式：400kbit/s
- 高速模式：3.4Mbit/s

### 2. EEPROM 概述

#### ① 设备地址
固定 `1010` + E1、E2、E3 三个引脚配置。

#### ② 写数据（一页内）
```
起始位 -> 发地址+写标记 -> 接收ACK -> 发EEPROM内部地址 -> 接收ACK -> 发送数据1 -> 接收ACK -> 发送数据2 -> 接收ACK ... -> 发送数据n -> 接收ACK -> Stop信号
```
> EEPROM 检测到 Stop 信号开始写（M24C02 的写周期 5ms）。

#### ③ 读数据（先伪写再读）
```
起始位 -> 发地址+写标记 -> 接收ACK -> 发EEPROM内部地址 -> 接收ACK -> 起始位 -> 发地址+读标记 -> 接收数据1 -> 发送ACK -> 接收数据1 -> 发送ACK ... -> 接收数据n -> 发送NACK --> Stop信号
```
> EEPROM 收到主设备发的 NACK 后会停止发送数据。

### 3. 使用 IIC 协议读写 EEPROM (软件寄存器方式)

#### ① IIC 驱动代码步骤
1. 初始化
2. 发送起始信号
3. 发送结束信号
4. 发送一个字节的数据
5. 接收应答信号
6. 接收一个字节的数据
7. 发送应答信号

#### ② EEPROM 部分代码
1. 向 EEPROM 的指定位置写入指定长度的数据。
2. 从 EEPROM 的指定位置读取指定长度的数据。

### 4. 使用 IIC 协议读写 EEPROM (硬件寄存器方式)

#### ① I2C 相关寄存器

| 寄存器名称 | 说明 | 相关控制位/标志位 |
| :--- | :--- | :--- |
| **CR1** | 控制寄存器1 | ACK: 应答使能<br>STOP: 停止信号产生<br>START: 开始信号产生<br>SMBus: 选择I2C模式(0)还是SMBus模式(1)<br>PE: I2C模块使能 |
| **CR2** | 控制寄存器2 | FREQ：I2C时钟频率，置范围2~36 |
| **DR** | 数据寄存器 | 高8位保留，低8位有效 |
| **SR1** | 状态寄存器1 | TxE: 数据寄存器为空(发送时)<br>RxNE: 数据寄存器非空(接收时)<br>BTF: 字节发送结束<br>ADDR: 地址已发送<br>SB: 起始位已发送 |
| **CCR** | 时钟控制寄存器 | FS: 选择标准模式(0)或快速模式(1)<br>CCR: 时钟控制分频系数(主模式) |
| **TRISE** | 升沿寄存器 | TRISE：的最大上升时间 (主模式) |

#### ② 初始化流程
```
1. 时钟使能
    1.1 GPIOB 模块时钟使能
    1.2 I2C2 模块时钟使能
    
2. PB10、PB11 都设置为复用开漏输出

3. I2C 配置
    3.1 选择I2C模式或SMBus模式
    3.2 设置I2C的时钟频率, 最大36MHz
    3.3 选择为标准模式（0:标准模式; 1:快速模式）
    3.4 设置标准模式下传输速度为100KHz
    3.5 设置允许的SCL的最大上升时间
    3.6 I2C2模块使能
```

### 5. 使用 IIC 协议读写 EEPROM (HAL 库方式)

#### 相关 HAL 库函数
- **HAL_I2C_Mem_Write()**: 以阻塞模式向指定的内存地址写入数据。
- **HAL_I2C_Mem_Read()**: 以阻塞模式从指定的内存地址读取数据。

---

## 附录：综合查询

### 1. 寄存器名称
- **RCC**: 复位和时钟寄存器 (Reset and Clock Control)
- **GPIOx**: 通用输入输出x寄存器 (General-Purpose Input/Output)
- **AFIO**: 复用功能输入输出寄存器 (Alternate Function Input/Output)
- **EXTI**: 外部中断寄存器 (External Interrupt)
- **NVIC**: 嵌套向量中断控制器 (Nested Vectored Interrupt Controller)
- **USART**: 通用同步/异步收/发 (Universal Synchronous/Asynchronous Receiver/Transmitter)

### 2. 名词缩写
- **AHB**: Advanced High-performance Bus (高性能总线)
- **APB**: Advanced Peripheral Bus (高级外设总线)
- **FSMC**: Flexible Static Memory Controller (灵活静态存储器控制器)
- **DMA**: Direct Memory Access (直接存储访问)
- **HSI**: High Speed Internal oscillator (高速内部时钟)
- **HSE**: High Speed External Oscillator/Clock (高速外部时钟)
- **PLL**: Phase Locked Loop (锁相环/倍频器)
- **LSI**: Low Speed Internal (低速内部时钟)
- **LSE**: Low Speed External oscillator (低速外部时钟)
- **HAL**: Hardware Abstraction Layer (硬件抽象层)
- **GPIO**: General-Purpose Input/Output (通用输入输出)
- **CMSIS**: Cortex Microcontroller Software Interface Standard (Cortex 微控制器软件接口标准)
- **AFIO**: Alternate Function I/O (替代功能输入输出)
- **SMBus**: System Management Bus (系统管理总线)

### 3. 单词表
```
assistant   助理、助手
optimize    优化
tick        发出滴答声
mask        掩饰、伪装、屏蔽
rising      上升
falling     下降
external    外部的
nest        嵌套
vector      向量
interrupt   中断
priority    优先级
universal   普遍的、通用的
Synchronous 同步的
Asynchronous 异步的
parity      奇偶校验
idle        闲置的
frequency   频率
peripheral  外设
```

### 4. I²C 和 SMBus 对比

| **特性** | **SMBus** | **I²C** |
| :--- | :--- | :--- |
| **通信速率** | 10 kbps - 100 kbps | 标准模式 100 kbps，快模式 400 kbps，高速模式最高可达 3.4 Mbps |
| **超时机制** | 从设备响应时间不能超过 35 毫秒 | 无强制超时要求 |
| **电源故障保护** | 支持低功耗和断电情况下的系统状态保护 | 无电源管理机制 |
| **报警信号线** | 支持 SMBALERT#，从设备可以主动报警 | 无报警信号线，通信必须由主设备发起 |
| **数据包格式和校验** | 更严格的格式和 CRC 校验 | 格式灵活，无强制的 CRC 校验 |
| **电压要求** | 3.0V - 5V | 1.8V - 5.5V，适应更多电压范围 |
| **应用场景** | 系统管理，如电源、温度、电池状态监控 | 通用设备间通信，如传感器、存储器 |

> SMBus 是 I²C 协议的一种变种， 专注于可靠性和状态监控，适用于电源管理和设备健康监控；而 I²C 具有较大的灵活性，适合通用的数据传输需求。