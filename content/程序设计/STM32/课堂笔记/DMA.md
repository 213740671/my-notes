# 电路原理

DMA是系统自带的传输功能  =>  不经过CPU  使用DMA可以减轻CPU的压力  同时传输的效率更高效

将存储器中的数据  =>   USART1的TX   =>  最终数据会传输到电脑的串口软件展示

# 内部寄存器

(1) 中断标记寄存器ISR:  传输错误中断   半传输中断  传输完成中断

(2) 清除中断标记寄存器IFCR: 置位1清除对应的中断标记   CGIFx可以一次清除当前通道所有的中断标记

(3**) CCR配置寄存器: 传输的方向   优先级   地址自增  数据宽度  使能  开中断**  初始化核心配置

(4) 数据长度寄存器CNDTR : 一次传输的数据长度

(5) CPAR外设地址寄存器: 存放当前传输的外设地址   =>  USART1_DR

(6) CMAR 存储器地址寄存器: 存放当前传输的存储器地址 => array

# 案例实现

## 初始化DMA1

(1) 打开DMA1的时钟

(2) 配置CCR寄存器  =>  传输数据的方向   存储器到外设

​	传输的优先级  => 统一都使用低

​	地址自增  => 存储器自增    外设不自增

​	数据宽度  => 8位

**(3) 打开USART1的DMA输出使能**

## 编写传输数据的函数

(1) 填写存储器和外设的地址

(2) 填写传输数据的长度

(3) 打开DMA的使能EN

## 特别注意

(1) 要想使用DMA, 需要同时配置DMA的通道和外设的DMA功能

(2) DMA是不受CPU影响的  一旦打开  CPU执行什么代码和DMA没有关系  比如延时

(3) 如果想要实现1s发送1次  =>   每次启动的时候发送一次  =>  

在发送完成的中断中  关闭DMA